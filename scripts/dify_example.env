# Dify Chat API 测试脚本配置示例
# 将此文件复制为 .env 并填入你的实际值

# Dify API 密钥（必需）
# 在 Dify 控制台的 "API 访问" 部分获取
# ⚠️ 重要：不要在客户端或版本控制中暴露此密钥
DIFY_API_KEY=your-api-key-here

# Dify API 基础 URL（可选）
# 云服务默认: https://api.dify.ai/v1
# 自部署示例: http://localhost/v1 或 http://your-domain.com/v1
DIFY_BASE_URL=https://api.dify.ai/v1

# 用户标识符（可选）
# 用于区分不同的用户会话，应用内唯一
# 建议使用用户的唯一 ID，如: user-12345
DIFY_USER=test-user

# 请求超时时间（秒）（可选）
# 注意：Cloudflare 超时限制为 100 秒
DIFY_TIMEOUT=60

# 响应模式（可选）
# streaming: 流式响应（推荐）
# blocking: 阻塞式响应（可能因长时间处理而被中断）
DIFY_RESPONSE_MODE=streaming

